<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <VersionSuffix>preview-0003</VersionSuffix>

    <Description>FluentWorkflow management support package</Description>
  </PropertyGroup>

</Project>
